# OpenAI-compatible API settings (used by `services/geminiService.ts`).
OPENAI_API_KEY=YOUR_API_KEY_HERE
OPENAI_API_BASE=https://api.openai.com/v1

# Prefer `MODEL_NAME`; `MODLE_NAME` is also supported for compatibility.
MODEL_NAME=gemini-3-pro-preview

